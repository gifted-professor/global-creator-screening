---
phase: 24-add-post-visual-review-positioning-card-analysis-step-for-approved-creators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app.py
  - backend/screening.py
  - scripts/run_keep_list_screening_pipeline.py
  - tests/test_visual_provider_diagnostics.py
  - tests/test_keep_list_screening_pipeline.py
autonomous: true
requirements:
  - POS-01
  - POS-02
  - POS-03
user_setup: []
must_haves:
  truths:
    - positioning-card analysis must run after visual review and only target creators whose visual status is already `Pass`
    - visual review remains the existing gate; Phase 24 fails if positioning-card analysis changes who is allowed into visual review or redefines Pass/Reject
    - approved creators must gain a structured brand-fit output that operators can inspect without reading raw model text
    - downstream summaries must show whether `positioning_card_analysis` ran, was skipped, or failed
  artifacts:
    - backend/app.py
    - backend/screening.py
    - scripts/run_keep_list_screening_pipeline.py
    - tests/test_visual_provider_diagnostics.py
    - tests/test_keep_list_screening_pipeline.py
  key_links:
    - `backend/app.py:perform_visual_review()` and the saved visual results already establish the post-visual boundary that Phase 24 must preserve
    - `scripts/run_keep_list_screening_pipeline.py` is the canonical repo-local downstream runner and must own the new stage summary
    - `backend/app.py:/api/artifacts/<platform>/status` and existing export helpers are the observable surfaces operators already inspect
---

<objective>
Add the first repo-local positioning-card analysis contract inside the existing downstream runtime so approved creators can receive structured positioning / brand-fit analysis after visual review.

Purpose: satisfy `POS-01` and `POS-02`, and establish the stage contract needed for `POS-03` without creating a terminal-only sidecar.
Output: a backend-owned positioning analysis artifact plus a downstream runner stage named `positioning_card_analysis`.
</objective>

<execution_context>
@/Users/a1234/Desktop/Coding/网红/chuhai/chuhaihai/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a1234/Desktop/Coding/网红/chuhai/chuhaihai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-add-post-visual-review-positioning-card-analysis-step-for-approved-creators/24-CONTEXT.md
@.planning/phases/23-wire-template-compiled-visual-prompts-into-runtime-and-define-visual-feature-group-contract/23-CONTEXT.md
@scripts/run_keep_list_screening_pipeline.py
@backend/app.py
@backend/screening.py
@tests/test_keep_list_screening_pipeline.py
@tests/test_visual_provider_diagnostics.py

<interfaces>
From `scripts/run_keep_list_screening_pipeline.py`:
```python
def run_keep_list_screening_pipeline(..., skip_scrape: bool = False, skip_visual: bool = False) -> dict[str, Any]:
```

Current per-platform summary shape already includes:
```python
platform_summary["visual_gate"] = {...}
platform_summary["visual_job"] = {...}
platform_summary["exports"] = export_platform_artifacts(...)
platform_summary["status"] = "completed" | "completed_with_partial_scrape" | ...
```

From `backend/app.py`:
```python
def load_profile_reviews(platform)
def save_profile_reviews(platform, profile_reviews)
def perform_visual_review(platform, payload, progress_callback=None, cancel_check=None)
@app.route("/api/jobs/visual-review", methods=["POST"])
@app.route("/api/artifacts/<platform>/status", methods=["GET"])
```

From `backend/screening.py`:
```python
def build_profile_review_record(...)
def resolve_visual_review_request_cover_limit(runtime_rules=None)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the positioning-card analysis contract and target-selection boundary</name>
  <files>backend/app.py, backend/screening.py</files>
  <read_first>backend/app.py, backend/screening.py, .planning/phases/24-add-post-visual-review-positioning-card-analysis-step-for-approved-creators/24-CONTEXT.md, .planning/phases/23-wire-template-compiled-visual-prompts-into-runtime-and-define-visual-feature-group-contract/23-CONTEXT.md</read_first>
  <action>Add backend-owned helpers for Phase 24 that make the contract explicit before wiring the stage. Create a persisted positioning artifact path per platform, plus helper(s) that select analysis targets only from creators who already have `visual_status == Pass` / equivalent saved visual-review success. Define one normalized record schema that is machine-readable and stable across providers. The schema must include concrete keys for: creator identifier, platform, positioning label list, fit recommendation, fit summary, evidence/signals list, prompt/provider/model metadata, reviewed timestamp, and the upstream visual status/source that admitted the row. Do not change how visual review determines Pass/Reject; positioning-card analysis is downstream-only.</action>
  <verify>rg -n "positioning_card|positioning_labels|fit_recommendation|fit_summary|evidence_signals|visual_status" backend/app.py backend/screening.py</verify>
  <acceptance_criteria>
    - `backend/app.py` or `backend/screening.py` contains `positioning_labels`
    - `backend/app.py` or `backend/screening.py` contains `fit_recommendation`
    - `backend/app.py` or `backend/screening.py` contains `fit_summary`
    - target-selection code explicitly filters on prior visual pass state
  </acceptance_criteria>
  <done>The runtime has an explicit positioning-card schema and a deterministic rule for which creators are eligible for analysis.</done>
</task>

<task type="auto">
  <name>Task 2: Implement backend positioning-card analysis using existing provider transport surfaces</name>
  <files>backend/app.py, backend/screening.py, tests/test_visual_provider_diagnostics.py</files>
  <read_first>backend/app.py, backend/screening.py, tests/test_visual_provider_diagnostics.py, .planning/phases/24-add-post-visual-review-positioning-card-analysis-step-for-approved-creators/24-CONTEXT.md</read_first>
  <action>Implement a dedicated backend analysis path for the new contract instead of overloading the visual-review JSON parser. Reuse the repo’s existing OpenAI-compatible provider abstraction, provider selection, retry semantics, and prompt-resolution conventions; do not create a separate terminal-only HTTP path. Add a positioning-card prompt builder that can work from repo-local runtime state first, without assuming the unavailable external benchmark directory exists. Expose a backend job surface such as `/api/jobs/positioning-card-analysis` and persisted artifact loading helpers so the downstream runner can trigger the step and later inspect saved results. Update `/api/artifacts/<platform>/status` or an adjacent artifact endpoint so positioning-card availability/path counts are observable. The first version should tolerate prompt absence by falling back to a local generic brand-fit prompt contract rather than blocking execution.</action>
  <verify>python3 -m unittest tests.test_visual_provider_diagnostics -v</verify>
  <acceptance_criteria>
    - `backend/app.py` contains `/api/jobs/positioning-card-analysis`
    - backend positioning results include provider/model metadata
    - backend artifact-status output includes a positioning-card availability or path field
    - `python3 -m unittest tests.test_visual_provider_diagnostics -v` exits 0
  </acceptance_criteria>
  <done>The backend can execute and persist positioning-card analysis with the existing provider stack and expose the resulting artifact through repo-local status surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Insert a non-blocking `positioning_card_analysis` stage into the keep-list downstream runner</name>
  <files>scripts/run_keep_list_screening_pipeline.py, tests/test_keep_list_screening_pipeline.py</files>
  <read_first>scripts/run_keep_list_screening_pipeline.py, tests/test_keep_list_screening_pipeline.py, backend/app.py, .planning/phases/24-add-post-visual-review-positioning-card-analysis-step-for-approved-creators/24-CONTEXT.md</read_first>
  <action>After the existing visual-review stage, wire a new stage named exactly `positioning_card_analysis` into `run_keep_list_screening_pipeline()`. Add an explicit skip control such as `--skip-positioning-card-analysis` and summary/preflight fields that make the stage observable beside scrape / visual / export. The runner must only start the new stage when visual review is already satisfied and there are approved creators to analyze. If there are zero passed creators, visual was skipped, or the operator disables the stage, record a skipped status with a concrete reason. If the stage errors, record `status=failed` on the stage payload plus warning/error details, but keep the overall platform/export path non-blocking by default unless another existing blocking condition already fails the run.</action>
  <verify>python3 -m unittest tests.test_keep_list_screening_pipeline -v</verify>
  <acceptance_criteria>
    - `scripts/run_keep_list_screening_pipeline.py` contains `positioning_card_analysis`
    - `scripts/run_keep_list_screening_pipeline.py` contains `--skip-positioning-card-analysis`
    - platform summaries include explicit `skipped`, `completed`, or `failed` stage values for positioning analysis
    - `python3 -m unittest tests.test_keep_list_screening_pipeline -v` exits 0
  </acceptance_criteria>
  <done>The canonical downstream runner now owns a visible, skippable, non-blocking positioning-card analysis stage after visual review.</done>
</task>

</tasks>

<verification>
- [ ] backend positioning analysis targets only visual-review-approved creators
- [ ] a persisted positioning-card artifact exists on the backend side and is discoverable via status surfaces
- [ ] `run_keep_list_screening_pipeline()` exposes a `positioning_card_analysis` stage with observable skip/fail/complete states
- [ ] `python3 -m unittest tests.test_visual_provider_diagnostics tests.test_keep_list_screening_pipeline -v` succeeds
</verification>

<success_criteria>
- All tasks completed
- `POS-01` is satisfied without altering the existing visual gate
- `POS-02` is satisfied with a stable machine-readable positioning result contract
- the new stage is repo-local, observable, and non-blocking by default
</success_criteria>

<output>
After completion, create `.planning/phases/24-add-post-visual-review-positioning-card-analysis-step-for-approved-creators/24-01-SUMMARY.md`
</output>
