---
phase: 21-replace-workbook-dashboard-project-home-runtime-paths-with-repo-local-implementations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - feishu_screening_bridge/bridge.py
  - feishu_screening_bridge/task_upload_sync.py
  - feishu_screening_bridge/__main__.py
  - feishu_screening_bridge/repo_local_runtime.py
  - tests/test_main_cli.py
  - tests/test_feishu_screening_bridge.py
autonomous: true
requirements:
  - DEP-01
  - DEP-02
user_setup: []
must_haves:
  truths:
    - Phase 21 does not succeed unless `import-from-feishu` and `sync-task-upload-view` can complete their default path without importing or writing into the external full `email` project.
    - Workbook download / compile work must reuse the repo's existing Feishu fetch and template-compile logic instead of creating a second parallel implementation.
    - "Dashboard" replacement in this milestone means a repo-local visibility surface owned by this repo; machine-readable summary plus local artifact references is acceptable, but the default path must stop depending on external `exports/index.html`.
    - Operator-facing payloads and CLI output must show resolved repo-local artifact paths explicitly so future automation can rely on them.
  artifacts:
    - feishu_screening_bridge/bridge.py
    - feishu_screening_bridge/task_upload_sync.py
    - feishu_screening_bridge/__main__.py
    - feishu_screening_bridge/repo_local_runtime.py
    - tests/test_main_cli.py
    - tests/test_feishu_screening_bridge.py
  key_links:
    - `feishu_screening_bridge/task_upload_sync.py` already owns task-upload workbook download, parse, and flexible compile behavior that should be promoted into the repo-local default path.
    - `feishu_screening_bridge/bridge.py` still centers on `build_screening_workbook_upload_bridge_payload(...)`, which is an external-project abstraction and should be replaced for the default path.
    - `scripts/prepare_screening_inputs.py` and `backend/app.py` already define repo-local patterns for rulespec persistence, runtime artifacts, and summary JSON output.
    - `tests/test_feishu_screening_bridge.py` already has fake Feishu clients and workbook fixtures that can lock the new repo-local import/sync contract without needing a sibling checkout.
---

<objective>
Replace the default workbook import and task-upload sync runtime path with repo-local implementations that own their own artifacts and operator summary surface.

Purpose: satisfy `DEP-01` and `DEP-02` by making workbook-related flows and their dashboard-style visibility surface run fully inside the current repository.
Output: repo-local import/sync helpers, default CLI routing that no longer depends on the external `email` project, and regression coverage for the new payload/summary contract.
</objective>

<execution_context>
@/Users/a1234/Desktop/Coding/网红/chuhai/chuhaihai/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a1234/Desktop/Coding/网红/chuhai/chuhaihai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-replace-workbook-dashboard-project-home-runtime-paths-with-repo-local-implementations/21-CONTEXT.md
@.planning/phases/20-baseline-legacy-dependency-surfaces-and-lock-repo-local-replacement-contract/20-LEGACY-DEPENDENCY-INVENTORY.md
@README.md
@feishu_screening_bridge/__main__.py
@feishu_screening_bridge/bridge.py
@feishu_screening_bridge/task_upload_sync.py
@backend/app.py
@scripts/prepare_screening_inputs.py
@tests/test_main_cli.py
@tests/test_feishu_screening_bridge.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a repo-local workbook import/sync runtime helper and artifact contract</name>
  <files>feishu_screening_bridge/repo_local_runtime.py, feishu_screening_bridge/bridge.py, feishu_screening_bridge/task_upload_sync.py</files>
  <read_first>feishu_screening_bridge/bridge.py, feishu_screening_bridge/task_upload_sync.py, scripts/prepare_screening_inputs.py, backend/app.py</read_first>
  <action>Create a repo-local helper module or equivalent internal helper surface that takes downloaded workbook inputs, compiles or flexibly parses them, persists repo-local artifacts, and writes a machine-readable summary/manifest owned by the current repo. Converge single-workbook import and task-upload bulk sync on this helper so the default path no longer flows through `build_screening_workbook_upload_bridge_payload(...)` or the external `email` DB. The returned payload must expose repo-local artifact paths for at least the downloaded workbook, parsed template outputs or rulespec outputs when available, and a summary/manifest JSON path.</action>
  <verify>rg -n "summary|manifest|repo_local|rulespec|templateParseArtifacts" feishu_screening_bridge/bridge.py feishu_screening_bridge/task_upload_sync.py feishu_screening_bridge/repo_local_runtime.py</verify>
  <acceptance_criteria>
    - default import/sync code path no longer requires `load_email_project(...)`
    - single-workbook import and task-upload sync both land repo-local artifact paths and a machine-readable summary/manifest
    - parsed template / rulespec outputs are reused from existing repo-local logic rather than rebuilt through an external-project abstraction
  </acceptance_criteria>
  <done>The repo owns workbook import state and artifact reporting instead of delegating it to the sibling `email` project.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire `import-from-feishu` and `sync-task-upload-view` to default to the repo-local path</name>
  <files>feishu_screening_bridge/__main__.py, feishu_screening_bridge/bridge.py, feishu_screening_bridge/task_upload_sync.py</files>
  <read_first>feishu_screening_bridge/__main__.py, feishu_screening_bridge/bridge.py, feishu_screening_bridge/task_upload_sync.py, .planning/phases/21-replace-workbook-dashboard-project-home-runtime-paths-with-repo-local-implementations/21-CONTEXT.md</read_first>
  <action>Change the CLI command handlers so `import-from-feishu` and `sync-task-upload-view` execute the new repo-local runtime path by default and only enter external-project compatibility mode when `--email-project-root` or `EMAIL_PROJECT_ROOT` is explicitly provided. Update non-JSON CLI output and JSON payloads so operators can see they are on the repo-local path and where the repo-local dashboard/summary artifacts landed. If the old external HTML dashboard is not produced on the default path, replace it with repo-local summary/manifests and clear next-step hints rather than silently preserving the old contract.</action>
  <verify>python3 -m unittest tests.test_main_cli -v</verify>
  <acceptance_criteria>
    - running the default command path with no legacy root uses repo-local execution instead of returning a "missing external root" failure
    - JSON payloads or CLI summaries expose repo-local mode and artifact locations
    - explicit legacy compatibility mode still remains available when an external root is intentionally supplied
  </acceptance_criteria>
  <done>Operators can keep using the legacy command names, but the default runtime ownership is now inside the current repo.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the repo-local workbook/dashboard contract into tests</name>
  <files>tests/test_main_cli.py, tests/test_feishu_screening_bridge.py</files>
  <read_first>tests/test_main_cli.py, tests/test_feishu_screening_bridge.py, feishu_screening_bridge/__main__.py, feishu_screening_bridge/bridge.py, feishu_screening_bridge/task_upload_sync.py</read_first>
  <action>Add or update tests so the default `import-from-feishu` and `sync-task-upload-view` path succeeds with repo-local fake fixtures, returns machine-readable repo-local artifact references, and does not require `CHUHAI_LEGACY_EMAIL_PROJECT_ROOT`. Keep external-project integration coverage explicit and optional, but extend unit coverage to assert the new repo-local payload/summary contract and CLI routing.</action>
  <verify>python3 -m unittest tests.test_main_cli tests.test_feishu_screening_bridge -v</verify>
  <acceptance_criteria>
    - targeted tests cover the new repo-local default path for both command families
    - tests assert repo-local artifact or summary paths instead of legacy external DB/dashboard outputs
    - optional legacy integration tests remain gated and do not block the repo-local default contract
  </acceptance_criteria>
  <done>The new repo-local workbook/dashboard path is protected by automated regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_main_cli tests.test_feishu_screening_bridge -v` succeeds
- [ ] default `import-from-feishu` and `sync-task-upload-view` no longer require an external root to run their repo-local path
- [ ] repo-local payloads expose downloaded workbook, parsed-template or rulespec outputs, and a summary/manifest path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- workbook import and task-upload sync default to repo-local ownership
- default operator-facing visibility surface is repo-local and no longer depends on the sibling project's dashboard export
- `DEP-01` and `DEP-02` are satisfied without regressing explicit compatibility mode

</success_criteria>

<output>
After completion, create `.planning/phases/21-replace-workbook-dashboard-project-home-runtime-paths-with-repo-local-implementations/21-01-SUMMARY.md`
</output>
