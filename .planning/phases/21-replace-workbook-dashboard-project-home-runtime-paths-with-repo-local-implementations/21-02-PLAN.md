---
phase: 21-replace-workbook-dashboard-project-home-runtime-paths-with-repo-local-implementations
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - feishu_screening_bridge/repo_local_runtime.py
  - feishu_screening_bridge/task_upload_sync.py
  - feishu_screening_bridge/bridge.py
  - feishu_screening_bridge/email_project.py
  - feishu_screening_bridge/__main__.py
  - tests/test_main_cli.py
  - tests/test_feishu_screening_bridge.py
autonomous: true
requirements:
  - DEP-03
user_setup: []
must_haves:
  truths:
    - Phase 21 is not complete unless the remaining project-home / workbench style runtime outputs are owned by this repo on the default path rather than rebuilt through external `project_home` and `project_workbench` modules.
    - The replacement does not need to reproduce the old read-model stack byte-for-byte; a stable repo-local manifest/index is acceptable only if it captures the operator-critical state those commands are meant to expose.
    - External `email` helpers may remain behind explicit compatibility mode, but the default path must stop calling them for project-home ownership.
    - The produced repo-local state must be machine-readable and future-automation-friendly, not just human-readable console text.
  artifacts:
    - feishu_screening_bridge/repo_local_runtime.py
    - feishu_screening_bridge/task_upload_sync.py
    - feishu_screening_bridge/bridge.py
    - feishu_screening_bridge/email_project.py
    - feishu_screening_bridge/__main__.py
    - tests/test_main_cli.py
    - tests/test_feishu_screening_bridge.py
  key_links:
    - `feishu_screening_bridge/task_upload_sync.py::_import_compiled_rows(...)` currently hard-codes the external project-home/workbench rebuild contract.
    - `feishu_screening_bridge/email_project.py` is now an explicit compatibility boundary and should stay that way; default repo-local flows should not need to load it.
    - Existing runner summaries under `scripts/run_task_upload_to_keep_list_pipeline.py`, `scripts/run_keep_list_screening_pipeline.py`, and `scripts/run_task_upload_to_final_export_pipeline.py` already define the repo's preferred machine-readable artifact style.
    - Phase 22 will validate bounded runtime stability and operator fallback docs, so this plan should focus on making the default ownership real rather than doing the final regression proof or runbook polish here.
---

<objective>
Replace the remaining external project-home/workbench ownership model with a repo-local project state contract and remove default external fallbacks from the decoupled path.

Purpose: satisfy `DEP-03` and finish the actual runtime decoupling work for Phase 21 so the current repository is the default owner of workbook/dashboard/project-home related flows.
Output: repo-local project state manifests/indexes, default-path removal of external project-home rebuild calls, and tests that prove explicit compatibility mode is optional rather than required.
</objective>

<execution_context>
@/Users/a1234/Desktop/Coding/网红/chuhai/chuhaihai/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a1234/Desktop/Coding/网红/chuhai/chuhaihai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-replace-workbook-dashboard-project-home-runtime-paths-with-repo-local-implementations/21-CONTEXT.md
@.planning/phases/21-replace-workbook-dashboard-project-home-runtime-paths-with-repo-local-implementations/21-01-PLAN.md
@feishu_screening_bridge/email_project.py
@feishu_screening_bridge/__main__.py
@feishu_screening_bridge/bridge.py
@feishu_screening_bridge/task_upload_sync.py
@scripts/run_task_upload_to_keep_list_pipeline.py
@scripts/run_keep_list_screening_pipeline.py
@scripts/run_task_upload_to_final_export_pipeline.py
@tests/test_main_cli.py
@tests/test_feishu_screening_bridge.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a repo-local project-state manifest/index that replaces the external project-home ownership model</name>
  <files>feishu_screening_bridge/repo_local_runtime.py, feishu_screening_bridge/task_upload_sync.py, feishu_screening_bridge/bridge.py</files>
  <read_first>feishu_screening_bridge/task_upload_sync.py, feishu_screening_bridge/bridge.py, scripts/run_task_upload_to_keep_list_pipeline.py, scripts/run_task_upload_to_final_export_pipeline.py</read_first>
  <action>Introduce a repo-local project-state contract for workbook/task-upload import results. The default flow should write a stable manifest or index owned by the current repo that captures, at minimum, task name or project code, downloaded workbook path, parsed template or rulespec outputs, resolved repo-local runtime locations, and operator next-step hints. Reuse the repo's existing summary/artifact naming patterns where possible so future orchestration work can consume the new state without translation.</action>
  <verify>rg -n "project_code|task_name|summary|manifest|next_step|artifacts" feishu_screening_bridge/repo_local_runtime.py feishu_screening_bridge/task_upload_sync.py feishu_screening_bridge/bridge.py</verify>
  <acceptance_criteria>
    - default repo-local import/sync path produces a stable machine-readable project-state artifact
    - artifact contains enough information to stand in for the old project-home/workbench visibility contract
    - artifact format aligns with existing repo-local summary style closely enough for later orchestration reuse
  </acceptance_criteria>
  <done>The repo has its own project-state surface instead of delegating project-home ownership to the sibling project.</done>
</task>

<task type="auto">
  <name>Task 2: Remove default external project-home rebuild calls while preserving explicit compatibility mode</name>
  <files>feishu_screening_bridge/task_upload_sync.py, feishu_screening_bridge/bridge.py, feishu_screening_bridge/email_project.py, feishu_screening_bridge/__main__.py</files>
  <read_first>feishu_screening_bridge/task_upload_sync.py, feishu_screening_bridge/bridge.py, feishu_screening_bridge/email_project.py, feishu_screening_bridge/__main__.py</read_first>
  <action>Eliminate default-path calls to external `ensure_project`, `import_requirements`, `rebuild_project_home_read_model`, `rebuild_project_workbench_read_model`, and `export_dashboard` when the operator is using the new repo-local path. Keep `email_project.py` as an explicit compatibility boundary only, and make sure the CLI/runtime wiring does not accidentally route repo-local execution through it. If compatibility mode remains available, it must be clearly opt-in and structurally separated from the default repo-local branch.</action>
  <verify>rg -n "rebuild_project_home_read_model|rebuild_project_workbench_read_model|export_dashboard|load_email_project" feishu_screening_bridge/task_upload_sync.py feishu_screening_bridge/bridge.py feishu_screening_bridge/__main__.py feishu_screening_bridge/email_project.py</verify>
  <acceptance_criteria>
    - default repo-local command path no longer relies on external project-home/workbench rebuild helpers
    - compatibility mode remains explicit and isolated behind `--email-project-root` / `EMAIL_PROJECT_ROOT`
    - default repo-local path does not silently fall back to the external project if repo-local work fails
  </acceptance_criteria>
  <done>The decoupled path becomes real ownership transfer, not just a wrapper over the old project-home stack.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests that prove repo-local project-home replacement is the default contract</name>
  <files>tests/test_main_cli.py, tests/test_feishu_screening_bridge.py</files>
  <read_first>tests/test_main_cli.py, tests/test_feishu_screening_bridge.py, feishu_screening_bridge/task_upload_sync.py, feishu_screening_bridge/bridge.py, feishu_screening_bridge/__main__.py</read_first>
  <action>Add targeted tests that assert the default repo-local import/sync path returns project-state manifest data and does not require or invoke the external compatibility loader. Use mocks or fake clients to prove the default branch stays repo-local, and keep any remaining external integration tests gated behind `CHUHAI_LEGACY_EMAIL_PROJECT_ROOT`.</action>
  <verify>python3 -m unittest tests.test_main_cli tests.test_feishu_screening_bridge -v</verify>
  <acceptance_criteria>
    - tests assert presence of repo-local project-state artifact references
    - tests prove the default branch does not need an external root and does not load compatibility mode implicitly
    - legacy integration coverage remains explicit and optional
  </acceptance_criteria>
  <done>The project-home replacement contract is enforced by tests, not left as an implementation assumption.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_main_cli tests.test_feishu_screening_bridge -v` succeeds
- [ ] default import/sync path returns repo-local project-state artifact references instead of external dashboard/read-model outputs
- [ ] explicit compatibility mode still works as an opt-in escape hatch, but is no longer required for the default contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- default workbook/dashboard/project-home path is fully repo-local
- project-home ownership is represented by repo-local machine-readable state, not the sibling project's read-model rebuild stack
- `DEP-03` is satisfied without reintroducing silent external fallbacks

</success_criteria>

<output>
After completion, create `.planning/phases/21-replace-workbook-dashboard-project-home-runtime-paths-with-repo-local-implementations/21-02-SUMMARY.md`
</output>
