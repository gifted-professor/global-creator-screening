---
phase: 14-stabilize-visual-provider-config-and-preflight
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - scripts/run_keep_list_screening_pipeline.py
  - scripts/run_screening_smoke.py
  - README.md
  - tests/test_main_cli.py
  - tests/test_keep_list_screening_pipeline.py
autonomous: true
requirements:
  - SCR-03
  - VIS-02
user_setup: []
must_haves:
  truths:
    - Keep-list downstream summaries tell operators which vision provider diagnostics applied to the run, not just whether visual-review later failed.
    - The official operator path documents the backend visual config surface and the difference between runner `--env-file` and backend `.env.local` loading.
    - Phase 14 leaves behind a bounded validation artifact that proves the new diagnostic contract is visible in the runner-facing summary.
  artifacts:
    - scripts/run_keep_list_screening_pipeline.py
    - scripts/run_screening_smoke.py
    - README.md
    - tests/test_main_cli.py
    - tests/test_keep_list_screening_pipeline.py
  key_links:
    - Runner summaries must consume backend-owned provider diagnostics rather than re-implementing provider parsing.
    - Documentation must explain the actual env precedence for backend visual-review config.
    - Validation in this plan is diagnostic-focused; the successful non-error live run remains Phase 15.
---

<objective>
Wire the new backend vision diagnostics into operator-facing runner summaries and docs for Phase 14.

Purpose: ensure operators can see the exact visual provider state in the same keep-list summary they already use, and leave behind a bounded validation artifact that proves the improved diagnostic contract.
Output: runner summary upgrades, docs clarifying visual config precedence, regression coverage, and a bounded diagnostic validation artifact.
</objective>

<execution_context>
@/Users/a1234/Desktop/Coding/网红/chuhai/chuhaihai/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a1234/Desktop/Coding/网红/chuhai/chuhaihai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-stabilize-visual-provider-config-and-preflight/14-CONTEXT.md
@.planning/phases/14-stabilize-visual-provider-config-and-preflight/14-01-PLAN.md
@backend/app.py
@scripts/run_keep_list_screening_pipeline.py
@scripts/run_screening_smoke.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist vision preflight diagnostics into keep-list and smoke summaries</name>
  <files>scripts/run_keep_list_screening_pipeline.py, scripts/run_screening_smoke.py, tests/test_keep_list_screening_pipeline.py</files>
  <read_first>backend/app.py, scripts/run_keep_list_screening_pipeline.py, scripts/run_screening_smoke.py</read_first>
  <action>Extend the runner summaries so they include backend-owned vision diagnostic/preflight data at both run-level and platform-level where appropriate. The summary should let an operator answer which providers were candidates, what the backend believed was configured, and why visual review was executed, skipped, or expected to fail. Add focused regression coverage for the summary shape.</action>
  <verify>python3 -m unittest tests.test_keep_list_screening_pipeline -v</verify>
  <acceptance_criteria>
    - keep-list runner summary contains richer vision diagnostics than the current `vision_providers` name list
    - smoke summary stays aligned enough that later debugging compares like with like
    - regression tests lock the summary contract so future refactors do not silently drop provider diagnostics
  </acceptance_criteria>
  <done>Operators can inspect visual provider preflight results directly from runner summary JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Document the real backend visual config precedence and operator checks</name>
  <files>README.md</files>
  <read_first>README.md, .env.example, backend/app.py, scripts/run_keep_list_screening_pipeline.py</read_first>
  <action>Update `README.md` to document the visual provider env surface, the current backend precedence (`.env.local` / process env versus runner `--env-file`), and the recommended bounded validation path before a live visual run. The docs must make it explicit how to tell whether a provider is actually configured and where to inspect that result in summary output.</action>
  <verify>rg -n \"OPENAI_VISION_MODEL|VISION_QUAN2GO|VISION_LEMONAPI|env.local|run_keep_list_screening_pipeline|visual\" README.md</verify>
  <acceptance_criteria>
    - README explains which files/env surfaces actually affect backend visual-review config
    - README gives at least one bounded validation command for the diagnostic contract
    - README points operators to the summary fields that explain visual provider readiness
  </acceptance_criteria>
  <done>Operator docs match the actual backend visual provider behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Produce a bounded diagnostic validation artifact for Phase 14</name>
  <files>scripts/run_keep_list_screening_pipeline.py, tests/test_main_cli.py</files>
  <read_first>scripts/run_keep_list_screening_pipeline.py, temp/keep_list_bounded_live_validation_escalated/summary.json</read_first>
  <action>Run a bounded diagnostic validation from the real keep workbook and persist a fresh summary under `temp/` that demonstrates the new vision diagnostic fields. This run does not need to be a successful non-error visual-review; it does need to prove that the runner summary exposes the provider/preflight state operators need for debugging.</action>
  <verify>python3 -m unittest tests.test_main_cli -v</verify>
  <acceptance_criteria>
    - a fresh bounded validation artifact lands under `temp/`
    - the new summary explicitly records visual provider diagnostic/preflight information
    - the artifact can be used as the handoff input for Phase 15's successful live validation
  </acceptance_criteria>
  <done>Phase 14 leaves behind a concrete diagnostic artifact instead of only code changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_keep_list_screening_pipeline -v` succeeds
- [ ] `python3 -m unittest tests.test_main_cli -v` succeeds
- [ ] A bounded diagnostic validation writes a fresh summary JSON under `temp/` with provider/preflight diagnostics
- [ ] `rg -n "OPENAI_VISION_MODEL|VISION_QUAN2GO|VISION_LEMONAPI|env.local|run_keep_list_screening_pipeline|visual" README.md` returns matches
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The keep-list runner and related docs expose backend vision provider diagnostics in an operator-usable way
- Phase 15 can focus on successful live validation rather than still discovering basic config ambiguity

</success_criteria>

<output>
After completion, create `.planning/phases/14-stabilize-visual-provider-config-and-preflight/14-02-SUMMARY.md`
</output>
