---
phase: 14-stabilize-visual-provider-config-and-preflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app.py
  - tests/test_visual_provider_diagnostics.py
  - scripts/test_runtime_validation.py
autonomous: true
requirements:
  - VIS-01
  - VIS-02
user_setup: []
must_haves:
  truths:
    - The backend can explain exactly which visual providers are configured, how they were resolved, and whether they are runnable before visual-review work starts.
    - Obvious vision-config problems are exposed as structured diagnostics rather than only as late `auth_not_found` row errors.
    - The backend keeps the existing visual-review execution path but upgrades preflight and operator-facing failure semantics.
  artifacts:
    - backend/app.py
    - tests/test_visual_provider_diagnostics.py
    - scripts/test_runtime_validation.py
  key_links:
    - Vision provider diagnostics must stay owned by `backend.app`, not by ad-hoc runner parsing.
    - `.env.local` loading behavior versus shell env must be explicit in the diagnostic output.
    - Phase 14 should improve diagnosability without promising the successful live run that Phase 15 owns.
---

<objective>
Add a backend-owned visual provider snapshot and preflight layer for Phase 14.

Purpose: make it possible to tell, before or at visual-job creation time, which provider/model/base URL/api style/auth source the backend will use and why a provider is or is not considered runnable.
Output: backend visual provider diagnostics, stricter preflight semantics, and regression/runtime validation coverage.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-stabilize-visual-provider-config-and-preflight/14-CONTEXT.md
@.env.example
@backend/app.py
@scripts/test_runtime_validation.py
@temp/keep_list_bounded_live_validation_escalated/summary.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a backend-owned visual provider snapshot contract</name>
  <files>backend/app.py</files>
  <read_first>backend/app.py, .env.example</read_first>
  <action>Extend the existing visual provider helpers in `backend/app.py` so the backend can produce a structured provider snapshot instead of only a name list. The snapshot should include at least provider name, env key, API key presence, base URL, model, API style, and whether the backend considers the provider runnable. It must make the `.env.local` auto-load behavior versus plain process env explicit so operators can see what source of truth the backend is actually using.</action>
  <verify>python3 -m unittest tests.test_visual_provider_diagnostics -v</verify>
  <acceptance_criteria>
    - backend exposes a reusable provider snapshot/preflight helper instead of duplicating logic in runners
    - the snapshot distinguishes “configured” from “present but suspicious/incomplete” where possible
    - provider diagnostics do not require a real network call to inspect local readiness
  </acceptance_criteria>
  <done>The backend can describe its visual provider state in structured form.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten preflight and early failure semantics for visual-review</name>
  <files>backend/app.py, tests/test_visual_provider_diagnostics.py</files>
  <read_first>backend/app.py, temp/keep_list_bounded_live_validation_escalated/summary.json</read_first>
  <action>Use the new provider snapshot/preflight information in the health and visual-review start paths so obvious config issues are surfaced earlier and more explicitly. The goal is not to guarantee a successful provider call yet, but to ensure that a bounded run no longer hides basic provider misconfiguration behind a vague late failure. Add regression tests for missing keys, fallback model behavior, and early error payloads.</action>
  <verify>python3 -m unittest tests.test_visual_provider_diagnostics -v</verify>
  <acceptance_criteria>
    - `/api/health` or an equivalent backend readiness surface returns richer vision diagnostics than just `configured/unconfigured`
    - visual-review start path exposes structured error details when no runnable provider exists
    - current success paths remain backward-compatible for downstream runners
  </acceptance_criteria>
  <done>Operators can detect local visual config problems before wasting time on a full review run.</done>
</task>

<task type="auto">
  <name>Task 3: Add repo-local validation coverage for the diagnostic contract</name>
  <files>tests/test_visual_provider_diagnostics.py, scripts/test_runtime_validation.py</files>
  <read_first>tests/test_main_cli.py, scripts/test_runtime_validation.py, backend/app.py</read_first>
  <action>Add focused regression tests for the new provider snapshot/preflight helpers and update runtime validation so the repo can report vision readiness with the richer contract. Keep validation local and deterministic; do not depend on live provider calls for this plan.</action>
  <verify>backend/.venv/bin/python scripts/test_runtime_validation.py</verify>
  <acceptance_criteria>
    - automated tests cover the new diagnostic fields and preflight branches
    - runtime validation prints or returns enough vision-readiness detail to help operator debugging
    - the validation surface remains compatible with existing screening backend smoke usage
  </acceptance_criteria>
  <done>The repo has repeatable coverage for backend visual provider diagnostics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests.test_visual_provider_diagnostics -v` succeeds
- [ ] `backend/.venv/bin/python scripts/test_runtime_validation.py` succeeds
- [ ] backend health/readiness surface now exposes richer vision provider diagnostics than only provider names
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Backend visual provider state is inspectable and preflighted before operators depend on live visual-review runs
- Basic config problems are surfaced earlier and more explicitly than the current late `auth_not_found` symptom

</success_criteria>

<output>
After completion, create `.planning/phases/14-stabilize-visual-provider-config-and-preflight/14-01-SUMMARY.md`
</output>
