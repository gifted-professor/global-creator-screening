# Quick Task 260331-h43 Summary

## Task

把本机默认视觉并发回调到 `6`，同时保留 `CLIProxyAPI openai provider` 的并发保护优化。

## What Changed

### 1. Local default moved back to 6

本机 [`.env.local`](/Users/a1234/Desktop/Coding/chuhaihai/.env.local) 已改回：

```env
VISUAL_REVIEW_MAX_WORKERS=6
```

同时保留：

```env
OPENAI_MAX_INFLIGHT_REQUESTS=8
```

也就是说：

- 默认外层 worker = `6`
- 本地 `openai` provider 级并发闸门 = `8`

### 2. Provider guard stays in code

在 [backend/app.py](/Users/a1234/Desktop/Coding/chuhaihai/backend/app.py) 中保留了本地 `openai` provider 的并发保护逻辑：

- 新增 `OPENAI_MAX_INFLIGHT_REQUESTS`
- 对 `127.0.0.1:8317` 默认使用本地 guard
- 在真正 `requests.post` 前获取 provider 级 request slot

这层优化同时作用于：

- 正式视觉复核调用
- provider probe 调用

## Verification

我用项目代码路径做了两轮 `6` 并发复测，输入是同一张真实 Instagram 封面图。

### Round 1

- `6` 并发
- `5/6` 成功
- 失败样本：`TLS handshake timeout`

判断：更像瞬时抖动，不像并发档位本身过高。

### Round 2

- `6` 并发
- `6/6` 成功
- `wall_time ≈ 8.07s`
- `median ≈ 5.68s`

## Conclusion

当前更稳的默认策略是：

- 默认并发：`6`
- 保留 provider 级 guard，给后续更高并发留保护层

如果后面再往上试，应该继续把 `6` 作为默认稳态，只把更高档位当作手动实验值。
