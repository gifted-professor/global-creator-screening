# Quick Task 260331-h43: 把本机默认视觉并发回调到 6，同时保留 CLIProxyAPI openai provider 的并发保护优化 - Context

**Gathered:** 2026-03-31
**Status:** Ready for execution

## Task Boundary

- 保留上一轮为本地 `CLIProxyAPI -> gpt-5.4` 增加的 provider 级并发保护
- 把这台机器的默认视觉并发从 `10` 调回 `6`
- 用项目代码路径复测 `6` 并发，而不是只看裸 HTTP

## Decisions

- 代码优化保留：继续允许外层 worker 提高时由 provider 级 gate 吸收抖动
- 本机默认值回到 `6`：优先稳态，而不是把默认配置顶到更激进档位
- 以项目代码路径下的真实并发复测结果作为最终判断依据
