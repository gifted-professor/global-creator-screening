# Quick Task 260331-gn2 Plan

## Goal

确认 `CLIProxyAPI` 暴露的本地 `gpt-5.4` HTTP 通道能否作为当前仓库的首选 GPT provider 候选。

## Tasks

1. 用真实 HTTP 请求验证本地 `chat/completions` 与 `responses` 两条链路都可用。
2. 对照当前仓库 `openai` vision provider 的请求方式，确认是否可以通过纯环境变量接入。
3. 记录结果、限制与推荐接入配置。

## Done When

- 至少一条文字请求和一条图片请求返回成功结果。
- 明确给出“可接 / 不可接 / 仍需补验证”的结论。
- quick task summary 留下可复用的接入配置建议。
