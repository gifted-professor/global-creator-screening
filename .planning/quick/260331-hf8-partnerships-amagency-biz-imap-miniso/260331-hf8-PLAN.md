# Quick Task 260331-hf8 Plan

1. 对 `imap.qq.com` 和 `imap.exmail.qq.com` 做原始 IMAP 登录与能力诊断
2. 解码共享邮箱在企业邮 IMAP 下暴露出来的真实目录结构
3. 用 repo-local `email_sync` 从共享邮箱的备份目录做最近三个月抽样抓信，并验证 `MINISO` 是否真实存在
4. 记录根因、可用入口和当前主线建议
